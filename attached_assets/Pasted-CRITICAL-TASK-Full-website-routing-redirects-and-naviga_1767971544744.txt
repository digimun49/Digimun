CRITICAL TASK: Full website routing, redirects, and navigation consistency audit & fix.

Project context:
- Static HTML/CSS/JS website
- Hosted on Netlify
- Clean URLs implemented (no .html visible)
- Uses Netlify _redirects
- Firebase auth for protected pages
- Multiple folders (e.g. /digimunx/)

CURRENT ISSUE:
- Internal navigation links use clean URLs (e.g. /login)
- Some clean URLs fail because routing, file structure, or redirects are inconsistent
- This is causing broken navigation and access issues

MANDATORY RULES:
- Do NOT remove clean URLs
- Do NOT reintroduce .html in frontend links
- Do NOT modify Firebase logic
- Do NOT guess paths — verify each one
- Fix root causes, not symptoms

STEP-BY-STEP INSTRUCTIONS:

1) Scan the entire project structure:
   - Identify where each page physically exists
   - Note root-level pages vs subfolder pages

2) Create a VERIFIED route map:
   Example:
   /login        → /login.html
   /signup       → /signup.html
   /digimunx     → /digimunx/index.html
   /digimunx/*   → protected content

3) Audit the Netlify `_redirects` file:
   - Ensure EVERY clean URL has a valid 200 rewrite
   - Ensure EVERY .html URL has a 301 redirect to clean URL
   - Ensure subfolder routes (e.g. /digimunx/) are correctly handled
   - Remove or fix any conflicting or missing rules

4) Audit ALL frontend navigation:
   - Navbar links
   - Buttons
   - Cards
   - JavaScript redirects (window.location, href, replace)
   - Ensure links point to VALID clean URLs that actually resolve

5) Special check for AUTH FLOW:
   - Login button
   - Signup button
   - Protected pages redirecting unauth users
   - Ensure unauth users are redirected to a WORKING clean login route

6) Validate behavior:
   - Visiting /login loads login page
   - Visiting /login.html redirects to /login
   - Clicking Login from ANY page works
   - Protected pages correctly redirect unauth users
   - No 404s or blank pages

7) After fixing:
   - Provide a clear report:
     - Pages checked
     - Redirects added/updated
     - Issues found and resolved

DEPLOYMENT RULE:
- Do NOT deploy from Replit
- Changes will be committed and pushed via GitHub only
