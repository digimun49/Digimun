You are working on a LIVE production website.

STRICT RULES (MANDATORY):
- Replit is used ONLY for code editing
- Netlify is used for deployment (DO NOT touch Netlify settings)
- Firebase backend already exists (DO NOT change Firebase console)
- DO NOT change Firebase collections structure
- DO NOT deploy anything
- DO NOT touch GitHub settings
- DO NOT break existing login/signup/admin logic

------------------------------------
CONFIRMED BACKEND STATUS (DO NOT CHANGE):
------------------------------------
Firebase Firestore already contains:
- Collection: reviews (documents already exist)
  Fields:
    - name
    - country
    - rating
    - message
    - status ("approved" / "pending")
    - createdAt (timestamp)

- Collection: tickets (frontend submit already implemented)
- Admin email: muneebg249@gmail.com
- Firebase rules already allow:
  - Public create for tickets & reviews
  - Admin full read/write access

BACKEND IS CORRECT. ISSUE IS FRONTEND ONLY.

------------------------------------
PRIMARY TASKS (FIX FRONTEND):
------------------------------------

### 1️⃣ REVIEWS NOT SHOWING ON WEBSITE (CRITICAL)
Fix frontend JavaScript so that:

- Reviews are READ from Firestore collection: "reviews"
- ONLY reviews with:
    status == "approved"
  are displayed publicly
- Use correct query:
    collection("reviews")
    where("status", "==", "approved")
    orderBy("createdAt", "desc")

- Ensure:
  - Correct Firebase import
  - Correct async handling
  - No console errors
  - Reviews render dynamically into DOM
  - Empty state handled gracefully

DO NOT hardcode reviews.
DO NOT generate fake reviews.

------------------------------------
### 2️⃣ ADMIN PANEL – REVIEWS MANAGEMENT
In admin.html / admin.js:

Admin must be able to:
- View ALL reviews (pending / approved / rejected)
- Approve review (status = "approved")
- Reject review (status = "rejected")
- Edit review message
- Delete review permanently

IMPORTANT:
- Reviews must NOT appear on public site unless approved
- Admin panel UI must be clean & professional
- Do NOT remove existing ticket management

------------------------------------
### 3️⃣ SUPPORT TICKETS – FRONTEND CHECK
Frontend help.html already submits tickets, but:

- Tickets are NOT appearing in admin panel
- Fix frontend JS to ensure:
  - Correct Firestore collection name: "tickets"
  - Proper create() call
  - Error handling + console logging
  - Admin panel correctly reads tickets

IMPORTANT:
- DO NOT add ticket submission inside admin panel
- Admin panel is ONLY for managing tickets

------------------------------------
### 4️⃣ NETLIFY ROUTING BUG (404 ISSUE)
Issue:
- URL like /digimunx/ loads for 1 second then shows Netlify 404

Fix:
- Ensure /digimunx/index.html exists
- Add proper frontend routing support:
  - Create _redirects file OR update netlify.toml
  - Route:
      /digimunx/*   /digimunx/index.html   200

DO NOT touch Netlify UI.
ONLY fix via files.

------------------------------------
### 5️⃣ ADMIN PANEL UI UPGRADE
Upgrade admin.html UI to HIGH-END PROFESSIONAL:

- Dark fintech theme
- Sidebar navigation:
    - Tickets
    - Reviews
    - Users (existing)
- Status badges (pending / approved / closed)
- Tables or cards
- Responsive layout
- Smooth hover & transitions

DO NOT remove existing admin features.

------------------------------------
### 6️⃣ DEBUGGING REQUIREMENTS
- Add console.log() where needed
- Clearly identify and fix:
  - Why reviews are not rendering
  - Why tickets are not appearing
- Keep code clean & readable

------------------------------------
FINAL REMINDER:
------------------------------------
- Backend is correct
- Firebase rules are correct
- Your job is ONLY frontend fixes
- Do NOT deploy
- Do NOT add fake data
- Do NOT break existing flows
