You are debugging and fixing the FRONTEND ONLY.

IMPORTANT CONTEXT:
- Replit is used ONLY for code editing
- Netlify handles deployment (DO NOT deploy)
- Firebase backend already exists
- Do NOT change Firebase console settings
- Do NOT add new authentication systems

OBJECTIVE:
Fix support ticket submission so tickets:
- Save correctly to Firestore
- Appear in admin.html
- Update ticket count correctly (if implemented)
- Show proper success/error messages

STRICT RULES:
- DO NOT modify Firebase rules
- DO NOT touch backend structure
- DO NOT add ticket submission in admin panel
- Admin panel is ONLY for handling tickets
- Users can ONLY submit tickets via help.html
- Preserve existing login/signup/admin logic

FRONTEND TASKS:
1. Verify help.html / help.js:
- Ensure tickets are saved to Firestore collection: "tickets"
- Fields must include:
- name
- email
- subject
- message
- status = "open"
- createdAt (serverTimestamp)

2. Ensure proper error handling:
- Log Firebase errors to console
- Show user-friendly messages on failure
- Show success message on submit

3. Verify admin.html / admin.js:
- Admin reads from collection: "tickets"
- Admin can:
- View all tickets
and from the tickets reply to them from admin.html and users also can see the replies from the website and after my reply user email automatically notified via the email user signed in or provided to me in the ticket also after my reply users can also reply to admin message or response till the ticket closed from one side either user or admin can close the ticket
- Update status (open / closed)
- Delete tickets
- NO ticket submission form in admin panel

4. Ensure frontend consistency:
- Collection name must match EXACTLY everywhere
- No duplicate or mismatched collection names
- No silent failures

5. Improve admin ticket UI:
- Clean table or card layout
- Status badges
- Action buttons (Close / Delete)

OUTPUT REQUIREMENTS:
- Only frontend code changes
- Clean, readable, professional UI
- No backend changes
- No deployment actions

If something is blocked by Firebase rules, STOP and LOG the issue instead of bypassing it.