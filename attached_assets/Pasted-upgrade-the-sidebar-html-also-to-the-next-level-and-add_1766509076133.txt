upgrade the sidebar html also to the next level and add the support ticket option evrywhere is need to You are working on an existing production trading website. 

STRICT RULES (MANDATORY):
- Do NOT change deployment, Netlify, or GitHub setup
- Do NOT add any paid services
- Do NOT remove existing authentication logic
- Replit is used ONLY for code editing
- Firebase Auth + Firestore are already in use
- Preserve all existing functionality
- Upgrade UI/UX + structure ONLY
- Do NOT break admin approval or forgot password flows

PROJECT CONTEXT:
- login.html and signup.html already exist
- Firebase Authentication is used
- Admin approval system already exists
- Forgot password already exists
- admin.html is used for admin control
- Netlify hosts frontend
- Firebase handles backend logic

GOAL:
Upgrade login.html and signup.html to a HIGH-END, PROFESSIONAL,
TRADING / FINTECH / ENTERPRISE-LEVEL EXPERIENCE
(similar to premium trading platforms & indicator companies)

----------------------------------
PART 1: LOGIN PAGE (login.html)
----------------------------------
Upgrade UI + UX without breaking logic:

- Modern, clean, premium design
- Dark trading theme (black / dark gray / neon accents)
- Smooth animations & transitions
- Professional typography
- Proper spacing & alignment

Functional requirements (MUST KEEP):
- Email login
- Password login

- Clear error messages:
  - Account not approved
  - Wrong password
  - User not found
- Loading states on login button
- Remember existing Firebase login logic

----------------------------------
PART 2: SIGNUP PAGE (signup.html)
----------------------------------
Upgrade signup to enterprise standard:

UI:
- Multi-step or well-structured form
- Clear labels & helper text
- Password strength indicator
- Trading-style professional layout

Functionality (MUST KEEP):
- Firebase signup
- User saved in Firestore
- Status set to "pending"
- Admin approval required before login
- Email verification if already implemented
- Validation messages without breaking flow

----------------------------------
PART 3: FORGOT PASSWORD (IMPORTANT)
----------------------------------
- Keep forgot password fully working
- Improve UI & messaging
- Professional confirmation message
- No logic removal
- Firebase reset email must still work

----------------------------------
PART 4: ADMIN APPROVAL SYSTEM (PRESERVE)
----------------------------------
- DO NOT break admin approval
- Login must still block non-approved users to acces any of our bot this code already exist you can check 
- Keep existing Firestore fields
- Improve code readability if needed

----------------------------------
PART 5: QUALITY & CODE STANDARDS
----------------------------------
- Clean, readable code
- Reusable CSS classes
- Minimal but professional animations
- No unnecessary libraries
- Mobile responsive
- Trading platform feel

----------------------------------
FILES TO EDIT ONLY:
- login.html
- signup.html and sidebar file 
- related JS files (ONLY if required)

DO NOT:
- Add new auth systems
- Add servers
- Change Firebase structure
- Deploy anything

ONLY upgrade UI/UX + code quality
while preserving ALL existing behavior.