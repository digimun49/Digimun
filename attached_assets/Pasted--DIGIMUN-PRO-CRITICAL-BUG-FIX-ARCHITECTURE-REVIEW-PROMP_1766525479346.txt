🔒 DIGIMUN PRO — CRITICAL BUG FIX & ARCHITECTURE REVIEW PROMPT (FOR REPLIT AGENT)

You are working on a LIVE production fintech / trading platform.

This is NOT a demo, NOT a free tool, and NOT a test project.

The site is hosted on Netlify, backend is Firebase (Auth + Firestore).
Replit is used ONLY for code editing.

🚨 CRITICAL ISSUE TO FIX (HIGHEST PRIORITY)

The page
https://digimun.pro/digimunx/

✔ briefly loads
❌ then crashes with infinite reload
❌ Safari/iOS shows: “A problem repeatedly occurred”
❌ Mobile + Desktop both affected

This indicates a routing / auth / redirect loop or JS crash.

🎯 YOUR PRIMARY TASK
Fix digimunx routing, authentication flow, and rendering

The page MUST:

Load reliably on mobile + desktop

NEVER reload itself infinitely

NEVER crash due to auth timing

NEVER depend on Replit-only behavior

Work correctly on Netlify production

🔐 ACCESS CONTROL (MANDATORY)

This is a premium, approval-only system.

Rules:

Only Firebase users with approved === true can access digimunx

Unapproved users MUST see a locked screen:

“Approval Pending” / “Access Denied”

No silent failures

Analyzer must NEVER appear broken

❌ NO guest access
❌ NO demo access
❌ NO auto unlock
❌ NO frontend-only checks

🔑 AUTHENTICATION FLOW (CRITICAL)

You MUST:

Wait for Firebase onAuthStateChanged

Only then:

Check user document

Check approved status

Render UI

❌ Do NOT:

Redirect inside auth listener loops

Reload page

Redirect to same route

✔ Use clean state rendering instead

🧠 FIX ALL POSSIBLE INFINITE LOOP CAUSES

You MUST inspect and fix:

window.location.href loops

location.reload() usage

Auth checks inside render loops

Relative script paths breaking in /digimunx/

Missing / absolute paths

JS errors causing reload fallback

Example fix:

<script src="/firebase.js"></script>


NOT:

<script src="firebase.js"></script>

📱 MOBILE & RESPONSIVE FIX (MANDATORY)

Current digimunx UI is:
❌ broken
❌ not responsive
❌ screen overflow / cutoff
❌ unusable on phone

You MUST:

Fix viewport meta

Remove overflow:hidden traps

Avoid 100vh iOS bugs

Ensure layout adapts to all devices

No screen tearing / stretching

⭐ REVIEWS SYSTEM – FIX
Problem:

Reviews exist in Firestore

Status = approved

NOT showing on website

NOT loading in admin panel

You MUST:

Fix Firestore query

Ensure:

where("status", "==", "approved")


Ensure auth does NOT block public reads

Add proper error handling

Render graceful empty states

🎫 SUPPORT TICKETS – FIX
Problem:

Tickets submit successfully

NOT loading in admin panel

You MUST:

Fix Firestore read query

Ensure admin auth is checked correctly

Ensure admin email:

muneebg249@gmail.com


has full access

DO NOT add ticket creation in admin panel

Admin panel is management only

🧩 ADMIN PANEL REQUIREMENTS

Admin must be able to:

View tickets

Reply YES / NO / text

Change ticket status

View & manage reviews

Approve / reject / edit reviews

Do NOT remove existing admin logic — improve it

🧱 CODE QUALITY RULES (STRICT)

You MUST:

Separate concerns:

auth.js

access.js

ui.js

Use async/await

Add defensive try/catch

Add console error logs for debugging

Avoid deeply nested logic

🔐 SECURITY EXPECTATIONS

Validate Firebase tokens properly

Prevent unapproved access

No token reuse abuse

Handle expired sessions gracefully

🚫 ABSOLUTELY DO NOT

❌ Change Firebase rules
❌ Change Netlify settings
❌ Deploy anything
❌ Add new backend
❌ Add paid services
❌ Add fake data
❌ Break login/signup/admin

🎯 FINAL GOAL

Deliver a stable, enterprise-grade premium tool:

✔ No crashes
✔ No reload loops
✔ Mobile & desktop stable
✔ Reviews working
✔ Tickets working
✔ Admin panel fully functional

This must feel like a REAL fintech SaaS, not a broken demo.

🚨 IMPORTANT

Fix issues systematically, not visually.
The problem is logic + routing, not just CSS.