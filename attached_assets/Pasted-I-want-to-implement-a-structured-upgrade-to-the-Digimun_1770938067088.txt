I want to implement a structured upgrade to the DigimunX website (HTML index page and backend system). Carefully follow all requirements below. If any backend architectural change is required, explain it clearly at the end before implementation.

1. Signal Recording System (User-Bound & Permanent)
	•	Every generated signal must be recorded permanently.
	•	Each signal must be bound to the authenticated user’s email.
	•	Store structured signal data only (pair, direction, time, reason, AI analysis output, result status, timestamps).
	•	Do NOT permanently store chart images.
	•	AI learning must be based only on structured stored reasoning and outcome data (not images).

⸻

2. Chart Screenshot Upload & AI Verification Flow
	•	User uploads trading chart screenshot.
	•	AI analyzes the chart and generates:
	•	Signal direction
	•	Entry time reference
	•	Structured reasoning
	•	Screenshot should be stored temporarily only for that signal workflow.
	•	Images must be compressed before temporary storage.
	•	Images must open only in a branded popup modal inside the site (no external links).
	•	After review lifecycle completes (explained below), images must be automatically deleted.

⸻

3. Signal ID & Admin Control
	•	Each signal must have a unique sequential ID.
	•	Only admin can see the true internal ID.
	•	Even if signals are deleted, the ID sequence must not break.
	•	Admin must be able to:
	•	Search signals by ID
	•	Search by pair name
	•	Edit direction, time, reason, or result
	•	Delete signals
	•	User must NEVER know if admin edited any signal.

⸻

4. Result Buttons Logic (Strict Workflow)

After AI generates a signal, show 4 buttons:
	•	WIN
	•	LOSS
	•	INVALID
	•	TRADE REFUNDED

Rules:
	•	User cannot upload a new chart until they select one of these options.
	•	If user refreshes or logs out/in:
	•	Pending signal must reappear.
	•	New analysis must remain locked.
	•	If user tries to bypass result submission → block new upload.

Auto-expiry:
	•	If no result submitted within 12 hours → signal auto-deletes.
	•	Admin can manually delete pending signals.

⸻

5. 15-Signal Batch Review System
	•	After every 15 completed signals (WIN/LOSS/INVALID/REFUNDED combined):
	•	Automatically group them into a hidden review batch.
	•	User must NOT know batches are fixed at 15.
	•	Batch sent to admin panel for review.
	•	Admin can:
	•	Edit signals
	•	Delete signals
	•	Approve batch
	•	User must NOT know if edits were made.

⸻

6. Admin Approval & Email System

After admin approval:
	•	Admin decides manually whether to send report email.
	•	If approved for email:
	•	Send professional HTML email (Digimun Pro fintech branding).
	•	Include:
	•	Performance summary
	•	Win/Loss statistics
	•	Link to user profile
	•	Attach PDF performance report.
	•	After email is sent:
	•	That batch’s screenshots must be permanently deleted.
	•	Structured signal data remains stored.

⸻

7. Performance Tracking (Frontend + Admin)

User “My Profile” must show:
	•	Total signals generated
	•	Wins
	•	Losses
	•	Invalid
	•	Refunded
	•	Overall performance %

Admin panel must allow:
	•	Search by email
	•	View full signal history
	•	Edit performance records
	•	Handle user profile management

User must be able to request full data deletion from profile section.

⸻

8. DigimunX Telegram Page Rename

Rename:
DigimunX Telegram → DigimunXLive

DigimunXLive must:
	•	Be a public marketing page.
	•	Show only admin-approved signals.
	•	Allow promotional videos.
	•	Display maximum last 50 signals with charts.
	•	If total signals exceed 50 → automatically delete older chart images.
	•	Only last 50 chart previews visible.

⸻

9. Image Storage Policy (Important)

Images must NOT be permanently stored.

Workflow:
	•	Compress image on upload.
	•	Store temporarily (suggest optimal storage method and justify it).
	•	Keep images only:
	•	Until batch review completes.
	•	Until next 15-signal batch replaces previous batch.
	•	Automatically delete old batch images once new batch completes.
	•	Ensure storage optimization and cost-efficiency.

At the end, explain clearly:
	•	What storage solution should be used
	•	Why it is optimal
	•	What backend changes are required

⸻

10. Strict Validation Rules
	•	System must not allow workflow bypass.
	•	No duplicate signal creation.
	•	No ID sequence corruption.
	•	No accidental email auto-send.
	•	Admin-only control for final approvals.
	•	Everything must function reliably without logic gaps.

If any architectural change is required in backend, list it clearly before implementation.